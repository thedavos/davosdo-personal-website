---
export const prerender = false;

const serviceParam = Astro.url.searchParams.get("service");
const serviceQuery = serviceParam
	? `?service=${encodeURIComponent(serviceParam)}`
	: "";

return Astro.redirect(`/${serviceQuery}#contacto`, 301);
---
